{% set name = "torchcam" %}
{% set version = "0.4.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/torchcam-{{ version }}.tar.gz
  sha256: c049de22378b6302a478142c8be00c536c53a80f49c1621c392808ab99980e3b

build:
  number: 0
  noarch: python
  script:
    ## Ensure exclusion of tests folder
    - rm -rf test  # [not win]
    ## Install package {{ name }} from PyPI
    - {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6
    - matplotlib-base >=3.0.0
    - numpy >=1.14.0
    - pillow >=8.3.2
    - pytorch >=1.5.1

test:
  imports:
    - torchcam
    - torchcam.methods
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/frgfm/torch-cam
  summary: Class activation maps for your PyTorch CNN models
  license: Apache-2.0
  license_file: LICENSE
  description: |
    Simple way to leverage the class-specific activation of 
    convolutional layers in PyTorch.
  doc_url: https://frgfm.github.io/torch-cam
  dev_url: https://github.com/frgfm/torch-cam

extra:
  recipe-maintainers:
    - sugatoray
